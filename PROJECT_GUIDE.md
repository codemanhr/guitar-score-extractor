# 吉他滚动曲谱提取应用项目指导

## 1. 项目目标

本项目要实现一个本地应用，用于从吉他演奏视频中提取视频下方实时滚动的曲谱区域，并将其去重、对齐、拼接成完整曲谱，最终导出为 PDF 文件。

应用的核心目标不是下载视频，也不是识别音符含义，而是从已有视频画面中恢复出完整、可阅读、可打印的谱面图像。

## 2. 使用场景

用户是一位吉他爱好者，经常观看带滚动曲谱的演奏视频。视频中的谱子会随着演奏实时滚动，用户无法直接获得完整 PDF。应用应允许用户导入本地视频，框选曲谱区域，然后自动提取并生成完整 PDF。

## 3. MVP 范围

第一阶段只实现本地视频处理，不实现视频平台 URL 抓取。

MVP 必须包含：

- 导入本地视频文件。
- 从视频中提取预览帧。
- 允许用户手动框选视频下方曲谱区域。
- 按固定频率抽帧。
- 裁剪每帧中的曲谱区域。
- 对裁剪结果做基础图像预处理。
- 估计相邻帧之间的滚动位移。
- 去除重复区域，只拼接新增曲谱内容。
- 生成完整长图。
- 将长图切分为 PDF 页面。
- 导出 PDF 文件。

MVP 暂不包含：

- 平台视频下载。
- 登录态处理。
- 绕过平台限制。
- 音符识别、节拍识别、MusicXML 导出。
- 自动生成可编辑乐谱。
- 云端处理。

## 4. 产品原则

- 本地优先：视频和中间处理结果默认只保存在用户本地。
- 用户可控：曲谱区域、抽帧频率、滚动方向、PDF 页面参数应可调整。
- 可预览：生成 PDF 前必须能预览拼接长图或关键中间结果。
- 可调试：失败时应保留日志和中间图像，方便定位匹配或拼接问题。
- 渐进增强：先把常见水平滚动谱稳定做好，再扩展自动检测、纵向滚动和复杂背景。

## 5. 推荐技术架构

推荐采用前后端分离的本地 Web 应用。

### 5.1 前端

建议使用：

- React
- Vite
- TypeScript

前端职责：

- 视频文件选择。
- 调用后端读取视频元信息。
- 显示视频预览帧。
- 提供曲谱区域框选交互。
- 提供处理参数配置。
- 显示处理进度。
- 预览拼接结果。
- 下载 PDF。

### 5.2 后端

建议使用：

- Python 3.11+
- FastAPI
- OpenCV
- NumPy
- Pillow
- FFmpeg
- reportlab 或 img2pdf

后端职责：

- 接收并管理本地视频文件。
- 读取视频元信息。
- 抽取预览帧。
- 按配置抽帧。
- 裁剪 ROI 区域。
- 图像预处理。
- 估计帧间位移。
- 去重拼接。
- 生成长图。
- 生成 PDF。
- 管理任务状态和日志。

### 5.3 目录结构建议

```text
backend/
  app/
    main.py
    api/
    services/
      video_service.py
      frame_extractor.py
      roi_cropper.py
      image_preprocessor.py
      stitcher.py
      pdf_exporter.py
    models/
    storage/
  tests/

frontend/
  src/
    components/
    pages/
    services/
    types/

storage/
  projects/
    <project-id>/
      project.json
      source/
      frames/
      crops/
      debug/
      stitched.png
      output.pdf
```

具体实现可以根据项目实际框架调整，但职责边界应保持清晰。

## 6. 核心处理流程

完整处理流程如下：

1. 用户导入本地视频。
2. 后端读取视频元信息，包括时长、分辨率、帧率。
3. 后端抽取一帧或多帧作为预览。
4. 用户在前端框选曲谱区域 ROI。
5. 用户设置处理参数。
6. 后端按指定 FPS 抽帧。
7. 后端对每帧执行 ROI 裁剪。
8. 后端对裁剪图做预处理。
9. 后端计算相邻裁剪图之间的滚动位移。
10. 后端判断新增区域并追加到全局画布。
11. 后端输出拼接长图。
12. 用户预览长图。
13. 后端按页面参数切分长图。
14. 后端导出 PDF。

## 7. 图像处理设计

### 7.1 ROI 裁剪

必须优先支持手动 ROI 框选。自动检测曲谱区域只能作为增强能力，不应成为 MVP 的前置依赖。

ROI 配置应保存为：

```json
{
  "x": 0,
  "y": 720,
  "width": 1920,
  "height": 260
}
```

坐标基于原始视频分辨率，而不是前端缩放后的预览尺寸。

### 7.2 抽帧策略

默认抽帧频率建议为每秒 2 到 5 帧。

参数应可配置：

- `sample_fps`
- `start_time`
- `end_time`
- `max_frames`

实现时应避免一次性把所有帧载入内存。长视频应采用流式处理或分批处理。

### 7.3 预处理

基础预处理包括：

- 灰度化。
- 对比度增强。
- 二值化。
- 去噪。
- 锐化。
- 黑底白字到白底黑字的可选转换。

预处理结果应可输出到 debug 目录，便于排查错误。

### 7.4 位移估计

MVP 优先支持水平滚动曲谱。

可选算法：

- OpenCV `phaseCorrelate`：用于估计整体平移。
- OpenCV `matchTemplate`：用于局部模板匹配。
- 边缘图匹配：减少颜色和背景变化影响。

实现要求：

- 应限制最大位移范围，避免误匹配。
- 应记录每次匹配的置信度。
- 低置信度时不应盲目拼接，应标记为可疑片段。
- 位移异常时应跳过或进入降级策略。

### 7.5 去重拼接

对于水平向左滚动的视频，画面右侧通常出现新的谱面内容。拼接逻辑应只追加新出现的部分，避免重复。

基础策略：

1. 比较相邻 ROI 裁剪图。
2. 估算当前帧相对上一帧的横向位移。
3. 根据位移提取新增条带。
4. 将新增条带追加到总画布。

必须避免简单地把所有帧完整横向或纵向排列，这会产生大量重复谱面。

### 7.6 长图生成

长图应保留足够分辨率，保证 PDF 可阅读。

建议输出：

- `stitched.png`：完整拼接图。
- `stitch_metadata.json`：包含每段来源帧、时间戳、位移、置信度、拼接位置。

## 8. PDF 导出设计

PDF 导出需要支持：

- A4 页面。
- Letter 页面。
- 横向和纵向。
- 页面边距。
- 缩放比例。
- 自动分页。

默认建议：

- A4。
- 纵向。
- 白色背景。
- 适合打印的边距。

长图切分时应避免把一行谱子切断。MVP 可以先按固定高度切分，后续再根据谱线位置智能分页。

## 9. 前端交互设计

MVP 页面建议包含：

- 项目首页或工作台。
- 视频导入区。
- 视频信息区。
- 预览帧区域。
- ROI 框选工具。
- 参数面板。
- 处理进度面板。
- 拼接结果预览。
- PDF 导出按钮。

参数面板建议包含：

- 抽帧 FPS。
- 起始时间。
- 结束时间。
- 滚动方向：自动、向左、向右、向上、向下。
- 黑白反转。
- 二值化阈值。
- PDF 页面尺寸。
- PDF 方向。
- PDF 边距。

## 10. 任务与状态模型

视频处理可能耗时较长，应使用任务模型。

任务状态建议：

- `pending`
- `extracting_frames`
- `cropping`
- `preprocessing`
- `stitching`
- `rendering_pdf`
- `completed`
- `failed`
- `cancelled`

任务进度应包含：

- 当前阶段。
- 当前处理帧数。
- 总帧数。
- 百分比。
- 日志摘要。
- 输出文件路径。

## 11. 数据模型建议

项目配置示例：

```json
{
  "id": "project-id",
  "source_video": "source/video.mp4",
  "video": {
    "width": 1920,
    "height": 1080,
    "fps": 30,
    "duration": 240
  },
  "roi": {
    "x": 0,
    "y": 760,
    "width": 1920,
    "height": 260
  },
  "processing": {
    "sample_fps": 3,
    "scroll_direction": "left",
    "invert": false,
    "threshold": "auto"
  },
  "pdf": {
    "page_size": "A4",
    "orientation": "portrait",
    "margin_mm": 10
  }
}
```

## 12. 资源需求

### 12.1 软件资源

必须资源：

- Python 3.11+
- FFmpeg
- OpenCV
- NumPy
- Pillow
- PDF 生成库

推荐资源：

- FastAPI
- Uvicorn
- React
- Vite
- TypeScript

可选资源：

- scikit-image
- SQLite
- Celery 或 RQ
- Tauri 或 Electron

### 12.2 硬件资源

最低建议：

- 8GB 内存。
- 普通多核 CPU。
- 足够的视频和中间帧存储空间。

推荐：

- 16GB 内存。
- SSD。
- 对 4K 或长视频处理时保留数 GB 到数十 GB 临时空间。

GPU 不是 MVP 必需资源。

### 12.3 测试资源

应准备以下样例视频：

- 水平滚动谱。
- 纵向滚动谱。
- 黑底白字谱。
- 白底黑字谱。
- 底部有字幕干扰的视频。
- 谱子透明叠加在演奏画面上的视频。
- 快速滚动和慢速滚动的视频。
- 有暂停、跳切、转场的视频。

每类视频应保留对应输出样例，便于回归测试。

## 13. 质量标准

拼接结果应满足：

- 曲谱内容完整。
- 重复区域尽量少。
- 谱线连续。
- 文字和指法标记可阅读。
- PDF 页面清晰。
- 处理失败时有明确错误信息。

实现质量要求：

- 后端处理逻辑应模块化。
- 图像处理函数应可单元测试。
- 长任务应有日志和进度。
- 不应把所有中间帧长期保存在内存。
- 不应把平台下载逻辑和曲谱处理逻辑耦合。

## 14. 开发里程碑

### 里程碑 1：项目骨架

- 创建前后端基础工程。
- 后端提供健康检查接口。
- 前端能连接后端。

### 里程碑 2：视频导入与预览

- 上传或选择本地视频。
- 读取视频元信息。
- 抽取预览帧。
- 前端显示预览帧。

### 里程碑 3：ROI 框选

- 前端支持框选曲谱区域。
- ROI 坐标能转换到原始视频分辨率。
- 后端能保存 ROI 配置。

### 里程碑 4：抽帧与裁剪

- 按配置抽帧。
- 批量裁剪 ROI。
- 输出 debug 图像。

### 里程碑 5：水平滚动拼接

- 实现帧间位移估计。
- 实现新增区域提取。
- 生成 `stitched.png`。

### 里程碑 6：PDF 导出

- 长图分页。
- 导出 PDF。
- 前端提供下载入口。

### 里程碑 7：稳定性增强

- 匹配置信度。
- 异常帧跳过。
- 黑白反转。
- 更多调参选项。
- 测试样例覆盖。

## 15. 后续增强方向

可在 MVP 稳定后扩展：

- 自动检测谱面区域。
- 自动判断滚动方向。
- 智能分页，避免切断谱行。
- 手动删除错误片段。
- 多段视频合并。
- 支持批量处理。
- 支持桌面应用封装。
- 支持 OCR 提取标题、调弦、速度等文本信息。
- 支持 MusicXML 或 Guitar Pro 导出，但这属于独立的大功能，不纳入 MVP。

## 16. 边界与合规

应用应处理用户本地已有且有权处理的视频。

项目不应优先实现以下能力：

- 绕过平台 DRM。
- 绕过登录或付费限制。
- 自动批量下载平台内容。
- 规避版权保护措施。

如果未来需要接入下载能力，应作为独立模块，并明确要求用户确认其拥有处理权限。

## 17. 后续实现约束

后续所有实现应遵循本文件：

- 新功能必须能映射到本文档中的 MVP、里程碑或增强方向。
- 涉及架构调整时，应先更新本文档再实现。
- 涉及核心算法替换时，应保留输入输出契约不变，或同步更新数据模型。
- 涉及资源依赖新增时，应记录新增依赖的用途。
- 涉及用户交互变化时，应保证 ROI 框选、预览、导出 PDF 的主流程不被破坏。

